## Project Rules (FE-03)

1. Forms must use react-hook-form + zod for validation.  
2. Every input must have a label and accessible error messages (aria-invalid, role="alert").  
3. Tests must be written with Jest + React Testing Library and executed before merging.  
